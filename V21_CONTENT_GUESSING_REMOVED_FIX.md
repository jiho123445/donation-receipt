# v21 — "내용 추측형 중복판정" 제거 (반복된 데이터 손실의 근본 원인 수정)

## 왜 v14~v20까지 계속 문제가 재발했는가

v13부터 이 문제를 "이름+날짜+금액+방법+내용을 조합한 지문(fingerprint)이 같으면
같은 후원건일 것이다"라는 **추측**으로 풀려고 했습니다. v14~v19는 그 추측 규칙에
계속 예외를 덧붙이는 방식으로 대응했지만, 추측인 이상 아래 두 가지 실패를
동시에 없앨 수 없었습니다.

- **오탐(데이터 손실)**: 실제로 다른 두 후원건이 우연히 이름·날짜·금액이 같으면
  하나가 조용히 사라짐 ("3건 중 2건만 표시").
- **미탐(중복 저장)**: 파일명이 조금만 바뀌어도(재다운로드, "(1)" 등)
  재업로드 판정에 실패해 같은 내용이 다시 쌓임.

## 실제 참고 사례 조사

같은 규모(150명 내외)의 국내 비영리단체가 실제로 쓰는 시스템을 확인했습니다.

- **국세청 홈택스 전자기부금영수증 일괄발급**: 엑셀 업로드 시 행 단위 내용
  기반 중복검증을 아예 하지 않습니다. "여러 번 나눠서 제출할 경우 중복발급
  검증절차 없이 발급됨"이라고 매뉴얼에 명시되어 있으며, 중복 방지는 제출자
  책임입니다.
- **도너스(Donus, 국내 비영리 후원관리 SaaS, 100만 기부자 데이터 처리)**:
  모든 납입(결제)은 시스템이 부여한 고유 납입코드로 식별됩니다. "한 번
  기부금영수증에 포함된 납입은 중복 발행이 되지 않습니다"라는 규칙도 내용을
  추측해서가 아니라, 납입코드-영수증 매칭 정보로 판단합니다. 후원자 중복은
  이름+이메일 또는 이름+휴대전화처럼 **명확한 값의 완전일치**로만 확인합니다.

즉, 실제로 검증된 시스템들은 "내용이 비슷하니 같은 건일 것이다"라고 추측하지
않습니다. 이번 수정은 이 원칙을 그대로 따랐습니다.

## v21에서 무엇을 바꿨는가

1. **`src/utils/donationDedup.ts` 전면 재작성**
   - 이름/날짜/금액/방법/내용 기반 지문 매칭을 완전히 제거했습니다.
   - Excel의 각 행은 파싱 시점에 이미 고유 id를 받으므로(`excelParser.ts`),
     이제는 그 id가 **정확히 같은 경우에만** 같은 레코드로 취급합니다.
   - 내용이 아무리 비슷해도 다른 id를 가진 두 행은 절대 하나로 합쳐지거나
     삭제되지 않습니다. **이제 "3건 중 2건만 표시"되는 일은 구조적으로
     발생할 수 없습니다.**

2. **파일 단위 재업로드 확인 (새로운 안전장치)**
   - 행 내용을 추측하는 대신, **파일 전체의 SHA-256 해시**로 "이 파일을
     이미 가져온 적이 있는지"를 명확하게 확인합니다.
   - 이미 가져온 파일과 100% 동일한 파일을 다시 올리면, 이전에 언제
     몇 건을 가져왔는지 보여주고 "그래도 다시 가져오기"를 명시적으로
     선택해야만 진행됩니다. (추측이 아니라 사실 확인 + 관리자 확인)
   - 내용을 한 글자라도 수정한 새 파일은 해시가 달라지므로 이 확인 없이
     정상적으로 추가됩니다.
   - 새 Firestore 컬렉션 `importedFiles`를 추가했습니다 (`firestore.rules`에
     동일한 로그인 기준 규칙 반영).

3. **`makeSourceKey()`(파일명 포함 해시) 제거**
   - 기존에는 파일명이 sourceKey 계산에 포함되어, 파일명이 바뀌면 재업로드
     판정이 틀어지는 문제가 있었습니다. 이제 sourceKey 개념 자체를
     제거했고, 대신 `sourceRow`(엑셀 원본 행 번호)를 참고용 정보로만
     남겼습니다. 어떤 식별/중복 판정에도 쓰이지 않습니다.

## 이번 수정에 따른 동작 변화 안내

- 이제 Excel의 유효한 행은 **항상, 예외 없이** 추가됩니다. 실수로 같은
  파일을 통째로 다시 올리는 경우만 확인 절차를 거칩니다.
- 만약 서로 다른 두 파일에 겹치는 후원건이 있다면(예: 8월 파일과 9월 파일에
  같은 후원건이 실수로 중복 기재된 경우) 시스템이 자동으로 걸러주지
  않습니다. 이는 홈택스와 동일한 정책이며, 150명 규모에서는 업로드 전
  담당자가 엑셀에서 육안으로 확인하는 것이 훨씬 안전합니다("추측으로
  삭제" vs "사람이 확인" 중 후자가 데이터 손실 위험이 없습니다).
- 기존에 v14~v20 사이에 잘못 병합되어 사라진 후원건은 코드 교체만으로
  자동 복구되지 않습니다. 원본 Excel 파일을 다시 한번 업로드해주세요.
  이번 버전부터는 재업로드해도 내용이 다르면 절대 조용히 걸러지지 않으므로
  안심하고 다시 올리셔도 됩니다.

## 적용 후 확인 방법

1. 이 zip으로 GitHub 저장소 전체를 교체하고 Vercel 재배포를 기다립니다.
2. 관리자로 로그인합니다.
3. (선택) 지금까지 데이터가 꼬였다고 의심되면, `엑셀 회원 명단 관리` 탭에서
   `회원 명단 초기화`로 `donations` 컬렉션을 깨끗이 비웁니다.
4. 실제 사용 중인 월별 Excel 파일들을 처음부터 순서대로 다시 업로드합니다.
5. 각 파일 업로드 후 뜨는 `엑셀에서 읽음 → 신규 저장 → 처리 후 누적` 숫자가
   실제 엑셀 행 수와 정확히 일치하는지 확인합니다.
6. 김지호(또는 다건 후원자) 검색 → 해당 연도 선택 → 상세 후원내역 건수가
   원본 엑셀과 일치하는지 확인합니다.
7. 같은 파일을 실수로 다시 선택해보고, "이미 가져온 파일" 확인창이 정상적으로
   뜨는지 확인합니다.
